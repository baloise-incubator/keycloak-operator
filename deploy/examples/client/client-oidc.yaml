apiVersion: keycloak.org/v1alpha1
kind: KeycloakClient
metadata:
  name: client-oidc
  labels:
    app: sso
spec:
  realmSelector:
    matchLabels:
      app: sso
  client:
    clientId: client-podc
    secret: client-secret
    clientAuthenticatorType: client-secret
    protocol: openid-connect